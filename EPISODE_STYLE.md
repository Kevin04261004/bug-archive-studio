# 에피소드 작성 기준

문제가 쉬워서 넘기는 게 아니라, 아무도 하지 않을 실수라서 넘긴다.
에피소드는 **컴파일러 최소 재현 코드**가 아니라 **사람이 실제로 저지르는 실수**여야 한다.

## 통과 기준

에피소드를 하나 쓸 때마다 세 질문에 전부 "예"가 나와야 한다.

1. **이 `before` 코드를 왜 썼는지 설명할 수 있나?**
   화면의 모든 줄에 의도가 있어야 한다. `Start()` 안에 `throw;` 한 줄만 있는 코드는
   의도가 없다. "정리하고 다시 던지려다 `catch` 대신 `finally`에 썼다"는 의도가 있다.

2. **작성자가 이게 동작할 거라 기대했을 법한가?**
   실수는 "될 줄 알았는데 안 되는 것"이다. 아무도 `int`를 인덱싱할 수 있다고
   기대하지 않는다. 하지만 `Length`가 배열인 줄 알고 인덱싱하는 건 실제로 일어난다.

3. **"나도 저거 해봤다"가 나오나?**
   이 시리즈의 통화는 난이도가 아니라 공감이다.

## 코드 규격

| 항목 | 값 |
|---|---|
| 줄 수 | **10–20줄**. 10줄 밑은 맥락이 없고, 20줄 위는 진단 줄이 진행자를 덮는다 |
| 한 줄 길이 | **38자 이하** 권장, **44자 하드 리밋**(넘으면 26px 밑으로 떨어져 렌더 거부) |
| `before` / `after` | 줄 수 동일, **정확히 한 줄만** 다름 |
| `focus_line` | 그 바뀐 줄 번호(1부터) |
| 오류 메시지 | 90자 내외. 진단 줄과 도감 카드에서 두 줄 안에 접혀야 함 |

### 왜 20줄이 상한인가

렌더러는 코드 카드 아래에 진단 줄과 진행 바를 놓고, 그 아래 y=1484부터 진행자 스프라이트를
그린다. 줄이 늘면 카드가 길어지면서 진단 줄이 진행자를 덮는다. 실측한 한계는 이렇다.

| 줄 수 | 결과 |
|---|---|
| ~20줄 | 줄 길이와 무관하게 안전 |
| 21–24줄 | 긴 줄(38자 이상)만 쓸 때에 한해 통과 |
| 25줄 이상 | 어떤 조합에서도 진행자와 겹침 |
| 29줄 이상 | `visible=min(count,28)` 때문에 화면 밖으로 잘림 |

30줄을 넣으려면 글자가 23px 아래로 내려가 폰에서 읽을 수 없다.
검증기가 실제 레이아웃을 계산해 이 선을 넘으면 오류로 잡는다.

## 이름 규칙

실험실 코드처럼 보이면 실패다.

- 금지: `class A`, `class B`, `void M()`, `foo`, `bar`, 한 글자 변수(`int a`, `T x`)
- 사용: 게임/앱에서 나올 이름 — `Player`, `Enemy`, `GameManager`, `Inventory`,
  `hp`, `speed`, `damage`, `slots`, `target`, `Spawn()`, `TakeDamage()`
- 제네릭 타입 매개변수만 예외로 `T` 허용(C# 관례)
- 파일 이름은 그 안의 주 타입과 맞춘다. `Player.cs`에 `class Enemy`가 있으면 안 됨

## 맥락 고르기

Unity 스크립트를 기본 무대로 쓴다. 시청자가 매일 보는 화면이기 때문이다.

- `Start()` / `Update()` / `Awake()` / `OnTriggerEnter()` 안
- `GetComponent<T>()`, `Instantiate()`, `Destroy()`, `Time.deltaTime`, `Debug.Log()`
- 인스펙터 필드(`public float speed = 5f;`), 코루틴, 리스트 순회, 세이브/로드

Unity에 안 어울리는 에러(어셈블리 alias, unsafe, 연산자 오버로드 등)는
콘솔 앱·유틸리티 클래스 맥락으로 간다. 그 경우에도 이름 규칙은 그대로다.

## 희귀 에러

실무에서 거의 안 나는 에러도 전부 제작한다. 다만 그때도 기준 1·2는 지킨다.
"이 에러를 내는 가장 짧은 코드"가 아니라 **"이 에러를 만나는 가장 그럴듯한 상황"**을 쓴다.
상황을 못 만들겠으면 그 에피소드는 보류하고 기록에 남긴다.

## 검증

```
python3 tools/validate_episodes.py
```

규격(줄 수, 폭, 단일 변경, focus_line, 메시지 길이)과 실험실 코드 냄새를 전부 검사한다.
`--strict`를 붙이면 냄새 경고도 실패로 취급한다.
