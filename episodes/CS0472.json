{
  "error_code": "CS0472",
  "message": "The result is always true since a value of type 'int' is never equal to null",
  "filename": "Health.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    public int hp = 100;",
    "",
    "    void Start()",
    "    {",
    "        if (hp != null)",
    "        {",
    "            Debug.Log(\"alive\");",
    "        }",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    public int hp = 100;",
    "",
    "    void Start()",
    "    {",
    "        if (hp > 0)",
    "        {",
    "            Debug.Log(\"alive\");",
    "        }",
    "    }",
    "}"
  ],
  "focus_line": 9,
  "music": "random",
  "bug": "../assets/CS0472.png"
}
