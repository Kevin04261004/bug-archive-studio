{
  "error_code": "CS1579",
  "message": "foreach cannot operate on 'int' because it has no GetEnumerator",
  "filename": "Spawner.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Spawner : MonoBehaviour",
    "{",
    "    public int count = 3;",
    "    public GameObject[] enemies;",
    "",
    "    void Start()",
    "    {",
    "        foreach (var foe in count)",
    "        {",
    "            Instantiate(foe);",
    "        }",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Spawner : MonoBehaviour",
    "{",
    "    public int count = 3;",
    "    public GameObject[] enemies;",
    "",
    "    void Start()",
    "    {",
    "        foreach (var foe in enemies)",
    "        {",
    "            Instantiate(foe);",
    "        }",
    "    }",
    "}"
  ],
  "focus_line": 10,
  "music": "random",
  "bug": "../assets/CS1579.png"
}
