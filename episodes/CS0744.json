{
  "error_code": "CS0744",
  "message": "Expected contextual keyword 'equals'",
  "filename": "Shop.cs",
  "before": [
    "using System.Linq;",
    "using UnityEngine;",
    "",
    "public class Shop : MonoBehaviour",
    "{",
    "    int[] ids = new int[2];",
    "    int[] owned = new int[2];",
    "",
    "    void Start()",
    "    {",
    "        var rows = from id in ids",
    "                join own in owned",
    "                    on id == own",
    "                select id;",
    "    }",
    "}"
  ],
  "after": [
    "using System.Linq;",
    "using UnityEngine;",
    "",
    "public class Shop : MonoBehaviour",
    "{",
    "    int[] ids = new int[2];",
    "    int[] owned = new int[2];",
    "",
    "    void Start()",
    "    {",
    "        var rows = from id in ids",
    "                join own in owned",
    "                    on id equals own",
    "                select id;",
    "    }",
    "}"
  ],
  "focus_line": 13,
  "music": "random",
  "bug": "../assets/CS0744.png"
}
