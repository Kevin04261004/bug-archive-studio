{
  "error_code": "CS0140",
  "message": "The label 'retry' is a duplicate",
  "filename": "Loader.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Loader : MonoBehaviour",
    "{",
    "    public int tries = 0;",
    "",
    "    void Start()",
    "    {",
    "retry:",
    "        tries++;",
    "        if (tries < 3)",
    "            goto retry;",
    "retry:",
    "        Debug.Log(\"Gave up\");",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Loader : MonoBehaviour",
    "{",
    "    public int tries = 0;",
    "",
    "    void Start()",
    "    {",
    "retry:",
    "        tries++;",
    "        if (tries < 3)",
    "            goto retry;",
    "done:",
    "        Debug.Log(\"Gave up\");",
    "    }",
    "}"
  ],
  "focus_line": 13,
  "music": "random",
  "bug": "../assets/CS0140.png"
}
