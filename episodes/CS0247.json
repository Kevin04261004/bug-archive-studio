{
  "error_code": "CS0247",
  "message": "Cannot use a negative size with stackalloc",
  "filename": "Pixels.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Pixels : MonoBehaviour",
    "{",
    "    public int width = 4;",
    "",
    "    unsafe void Scan()",
    "    {",
    "        int* buffer = stackalloc int[-1];",
    "        Debug.Log(width);",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Pixels : MonoBehaviour",
    "{",
    "    public int width = 4;",
    "",
    "    unsafe void Scan()",
    "    {",
    "        int* buffer = stackalloc int[4];",
    "        Debug.Log(width);",
    "    }",
    "}"
  ],
  "focus_line": 9,
  "music": "random",
  "bug": "../assets/CS0247.png"
}
