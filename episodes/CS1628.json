{
  "error_code": "CS1628",
  "message": "Cannot use ref or out parameter 'hp' inside a lambda expression",
  "filename": "Health.cs",
  "before": [
    "using System;",
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    void Bump(ref int hp)",
    "    {",
    "        int copy = hp;",
    "        Action log = () =>",
    "            Debug.Log(hp);",
    "        log();",
    "    }",
    "}"
  ],
  "after": [
    "using System;",
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    void Bump(ref int hp)",
    "    {",
    "        int copy = hp;",
    "        Action log = () =>",
    "            Debug.Log(copy);",
    "        log();",
    "    }",
    "}"
  ],
  "focus_line": 10,
  "music": "random",
  "bug": "../assets/CS1628.png"
}
