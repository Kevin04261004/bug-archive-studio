{
  "error_code": "CS1026",
  "message": ") expected",
  "filename": "Health.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    public int hp = 40;",
    "    public int max = 100;",
    "",
    "    public void AddHealth(int heal)",
    "    {",
    "        hp = Mathf.Min(hp + heal, max;",
    "        Debug.Log(\"HP: \" + hp);",
    "    }",
    "",
    "    public bool IsFull()",
    "    {",
    "        return hp == max;",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Health : MonoBehaviour",
    "{",
    "    public int hp = 40;",
    "    public int max = 100;",
    "",
    "    public void AddHealth(int heal)",
    "    {",
    "        hp = Mathf.Min(hp + heal, max);",
    "        Debug.Log(\"HP: \" + hp);",
    "    }",
    "",
    "    public bool IsFull()",
    "    {",
    "        return hp == max;",
    "    }",
    "}"
  ],
  "focus_line": 10,
  "music": "random",
  "bug": "../assets/CS1026.png"
}
