{
  "error_code": "CS0269",
  "message": "Use of unassigned out parameter 'count'",
  "filename": "Inventory.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Inventory : MonoBehaviour",
    "{",
    "    public bool TryTake(out int count)",
    "    {",
    "        Debug.Log(\"Taking \" + count);",
    "        count = 1;",
    "        return true;",
    "    }",
    "",
    "    void Start()",
    "    {",
    "        TryTake(out int taken);",
    "    }",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Inventory : MonoBehaviour",
    "{",
    "    public bool TryTake(out int count)",
    "    {",
    "        Debug.Log(\"Taking one\");",
    "        count = 1;",
    "        return true;",
    "    }",
    "",
    "    void Start()",
    "    {",
    "        TryTake(out int taken);",
    "    }",
    "}"
  ],
  "focus_line": 7,
  "music": "random",
  "bug": "../assets/CS0269.png"
}
