{
  "error_code": "CS0455",
  "message": "Type parameter 'K' inherits conflicting constraints 'struct' and 'class'",
  "filename": "Cache.cs",
  "before": [
    "using UnityEngine;",
    "",
    "public class Cache<K, V>",
    "    where K : class, V",
    "    where V : struct",
    "{",
    "    public K key;",
    "    public V value;",
    "}",
    "",
    "public class Store : MonoBehaviour",
    "{",
    "    Cache<string, int> cache = new();",
    "}"
  ],
  "after": [
    "using UnityEngine;",
    "",
    "public class Cache<K, V>",
    "    where K : class, V",
    "    where V : class",
    "{",
    "    public K key;",
    "    public V value;",
    "}",
    "",
    "public class Store : MonoBehaviour",
    "{",
    "    Cache<string, int> cache = new();",
    "}"
  ],
  "focus_line": 5,
  "music": "random",
  "bug": "../assets/CS0455.png"
}
